import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './20_React动画实现/03_TransitionGroup/App';
import {StrictMode} from "react";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    // <StrictMode>
        <App name='why'/>
    // </StrictMode>
);

